{
  "name": "pi-providers-manager",
  "version": "0.3.1",
  "description": "Interactive TUI manager for pi providers, models, and subagents (/providers + /agents commands)",
  "keywords": [
    "pi-package",
    "pi-extension",
    "providers",
    "models",
    "agents",
    "tui"
  ],
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "license": "MIT",
  "author": "Earendil",
  "repository": {
    "type": "git",
    "url": "https://github.com/piexian/pi-providers-manager.git"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "dependencies": {
    "comment-json": "^4.2.5",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^24.0.0"
  }
}
